import 'package:flutter/material.dart';

class AppColors {
  // ── VOID-TO-NEON SPECTRUM (Stitch Design System) ──
  static const Color background = Color(0xFF090E1C);
  static const Color sidebar = Color(0xFF0D1323); // surface-container-low
  static const Color surface = Color(0xFF13192B); // surface-container
  static const Color surfaceBright = Color(0xFF242B43);
  static const Color surfaceBorder = Color(0x26707588); // outline with 15% opacity

  // ── BRAND NEON COLORS ──
  static const Color primary = Color(0xFF6DDDFF); // Electric Blue
  static const Color primaryDim = Color(0xFF00C3EB);
  static const Color secondary = Color(0xFF50E1F9); // Cyan
  static const Color success = Color(0xFF00E676);
  static const Color warning = Color(0xFFFDB022);
  static const Color danger = Color(0xFFFF8257); // Orange-Amber (Tertiary in Stitch)
  static const Color error = Color(0xFFFF716C);

  // ── NEUTRAL TEXT ──
  static const Color textPrimary = Color(0xFFE1E4FA); // on-surface
  static const Color textSecondary = Color(0xFFA6AABF); // on-surface-variant
  static const Color textDisabled = Color(0xFF505466); // inverse-on-surface
}
